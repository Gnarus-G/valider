{
  "name": "valider",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@types/jest": "^27.4.0",
    "ts-jest": "^27.1.3",
    "ts-node-dev": "^1.1.8",
    "typescript": "^4.5.5"
  },
  "scripts": {
    "build": "npm run build -ws",
    "test": "npm run test -ws"
  },
  "version": "0.1.0"
}
